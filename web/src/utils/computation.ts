import {Data, Solution} from '../types'

export function solve(data:Data){    
    let lCycle:number = data.params?.length ?? data.elements.length

    
}
export function get_combination_space(data:Data){
    // if length of cycle is defined manually use it, otherwise use the length of elements
}
